<%= form_for(@appraisal_field_datum) do |f| %>
  <% if @appraisal_field_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@appraisal_field_datum.errors.count, "error") %> prohibited this appraisal_field_datum from being saved:</h2>

      <ul>
      <% @appraisal_field_datum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data %><br>
    <%= f.text_field :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
